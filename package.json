{
  "name": "ts-training",
  "version": "1.0.0",
  "main": "build/index.js",
  "license": "MIT",
  "scripts": {
    "ts-node": "ts-node",
    "build": "tsc",
    "clean": "rimraf build",
    "start": "rimraf build && tsc --watch",
    "calculate": "node build/index"
  },
  "devDependencies": {
    "@types/node": "^17.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "axios": "^0.24.0"
  }
}
